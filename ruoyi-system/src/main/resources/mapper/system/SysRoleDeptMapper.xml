<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysRoleProjectMapper">

	<resultMap type="SysRoleProject" id="SysRoleProjectResult">
		<result property="roleId"     column="role_id"      />
		<result property="projectId"     column="project_id"      />
	</resultMap>

	<delete id="deleteRoleProjectByRoleId" parameterType="Long">
		delete from sys_role_project where role_id=#{roleId}
	</delete>
	
	<select id="selectCountRoleProjectByProjectId" resultType="Integer">
	    select count(1) from sys_role_project where project_id=#{projectId}
	</select>
	
	<delete id="deleteRoleProject" parameterType="Long">
 		delete from sys_role_project where role_id in
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId}
        </foreach> 
 	</delete>
	
	<insert id="batchRoleProject">
		insert into sys_role_project(role_id, project_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.roleId},#{item.projectId})
		</foreach>
	</insert>
	
</mapper> 